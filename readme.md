# Build

```bash
mvn clean package
```

# Docker

```
mvn clean package
cd dev-station
docker-compose pull
docker-compose up --build
```

# Deployment

api.dependency-manager.oups.net
www.dependency-manager.oups.net
